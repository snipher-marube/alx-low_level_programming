#include <stdio.h>

/**
 * main - print thr name of the file
 * @argc: first argument
 * @argv: second agument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);
	return (0);
}
